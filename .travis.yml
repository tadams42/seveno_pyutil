dist: xenial
language: python
python:
  - '3.9'
  # - 'pypy3'
script:
  - pip install .[dev]
  - coverage run --source seveno_pyutil -m pytest -v
branches:
  only:
    - development
    - master
    - "/^feature.*$/"
